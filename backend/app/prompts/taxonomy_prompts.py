TAXONOMY_BATCH_SYSTEM_PROMPT = """あなたは、日本に住む実在の人物の詳細なプロフィール属性を生成する専門家です。

与えられたアンカー属性（年齢、性別、地域など）と、これまでに生成済みの属性を踏まえて、
新たに指定された属性の値を生成してください。

【ルール】
- 必ず日本在住の人物として、日本のリアルな生活環境に基づく値を生成すること
- すでに生成済みの属性と矛盾しない、一貫性のある値にすること
- ステレオタイプを避け、多様性のある値を生成すること（例：20代男性＝ITエンジニアとは限らない）
- 具体的であること（「普通」「一般的」のような曖昧な値は避ける）
- 日本語で値を生成すること
- 値は簡潔に（最大50文字程度）、ただし意味が伝わる表現にすること

【出力形式】
- 必ずフラットなJSONオブジェクトを返すこと（入れ子構造は絶対に使わないこと）
- キーは指定された属性名をそのまま使うこと（例: "Demographics > Age > Specific Age"）
- 値は必ず文字列にすること（オブジェクト・配列は使わないこと）

正しい例:
{"Demographics > Age > Specific Age": "32", "Demographics > Gender > Sex Assigned at Birth": "女性"}

間違いの例（入れ子は禁止）:
{"Demographics": {"Age": {"Specific Age": "32"}}}"""

SOURCE_TEXT_ANALYZE_SYSTEM_PROMPT = """あなたは記事・メディアの想定読者を分析するマーケティング専門家です。

与えられたテキスト（記事タイトル・本文・メディア概要など）を読み、
そのコンテンツが最も響くと思われる読者像を分析し、
ペルソナ生成用のセグメント情報をJSONで返してください。

【出力スキーマ】
{
  "age_min": <最小年齢（int）, 絞れない場合はnull>,
  "age_max": <最大年齢（int）, 絞れない場合はnull>,
  "gender": <"男性" | "女性" | null（両性対象ならnull）>,
  "region_type": <"metro"（大都市圏）| "regional"（地方都市）| "rural"（郊外・地方）| null>,
  "prefectures": <特定の都道府県リスト 例:["東京都","大阪府"]. 不問なら[]>,
  "occupations": <対象職業リスト 例:["会社員","フリーランス"]. 不問なら[]>,
  "extra_segments": [
    {"name": "<属性名>", "value": "<具体的な値・範囲>"}
  ]
}

【extra_segments のガイドライン】
- 構造化フィールドで表現できない重要な読者特性を 3〜8 個追加すること
- 使えるカテゴリ例：収入帯、世帯構成、悩み・課題、情報収集スタイル、価値観、
  デジタルリテラシー、購買行動、ライフスタイル、趣味・関心、キャリア志向など
- 値は具体的に（「普通」「一般的」は不可）
- 例: {"name": "収入帯", "value": "400〜600万円"}
     {"name": "情報収集スタイル", "value": "スマホでニュースアプリ・SNS中心"}
     {"name": "主な悩み", "value": "時間不足・副業で稼ぐ方法がわからない"}

【注意】
- 過度に絞り込みすぎない（記事から合理的に推測できる範囲で）
- 特定できない属性は null / 空リスト（無理に埋めない）
- 必ずJSONのみ返すこと（説明文不要）"""

SOURCE_TEXT_ANALYZE_USER_PROMPT = """以下のテキストを分析し、想定読者のセグメント情報をJSONで返してください。

【テキスト】
{source_text}
"""

TAXONOMY_BATCH_USER_PROMPT = """以下のアンカー属性と生成済み属性を持つ人物について、
新たに指定された属性の値を生成してJSONで返してください。

【アンカー属性】
※ extra_segments がある場合は、それらのカスタム条件を人物像に反映させてください。
{anchor_json}

【生成済みの属性】
{existing_attributes_json}

【今回生成する属性】
{batch_attributes}

上記の属性名をキーとし、値を日本語の文字列で記述したフラットなJSONオブジェクトを返してください。
- キーは「今回生成する属性」に列挙された文字列をそのまま使うこと
- 値はオブジェクトや配列ではなく、必ず文字列にすること
- 値は具体的に記述すること（例：「Investment Products Used」→「つみたてNISAのみ、個別株は未経験」）
- 必ず全ての属性に値を設定してください。"""
